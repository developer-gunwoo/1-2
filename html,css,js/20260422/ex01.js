function solution(num1, num2) {
    return Math.floor(num1/num2);
}

// Document 브라우저 프론트엔드 html
// Math Object 자료형 node 기본 지원 되는 객체
// round 반올림
// floor 내림
// ceil 올림
// random() 0<x<1 사이 숫자 반환

const ret = solution(10, 5); //2
console.log(ret);